import { UnitFilterPipe } from './unit-filter.pipe';

describe('UnitFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
